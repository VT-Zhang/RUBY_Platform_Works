<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <title>Edit Dojo</title>
    </head>
    <style>
        .error{
            color:red;
        }
    </style>
    <body>
        <h1>Editing <%= @dojo.branch %> Dojo</h1>
        <form action="/dojos/<%= @dojo.id %>" method="post">
            <input type="hidden" name="_method" value="patch" />
            <input type="hidden" name="authenticity_token" value="<%= form_authenticity_token %>"/>
            <p>Branch: <input type="text" name="branch" value="<%= @dojo.branch %>"/></p>
            <p>Street: <input type="text" name="street" value="<%= @dojo.street %>"/></p>
            <p>City: <input type="text" name="city" value="<%= @dojo.city %>"/></p>
            <p>State: <input type="text" name="state" value="<%= @dojo.state %>"/></p>
            <p><input type="submit" value="update Dojo" /></p>
        </form>
        <p><a href="/dojos/">Home</a> | <a href="">Show</a></p>

        <% if flash[:errors] !=nil %>
        <% flash[:errors].each do |error| %>
        <p class="error">
            <%= error %>
        </p>
        <% end %>
        <% end %>
    </body>
</html>
